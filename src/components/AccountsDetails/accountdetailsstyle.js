

export const accountdetailsstyle = {
    membership: (sm) => ({
        minWidth: `227px`,
        minHeight: `296px`,
    }),
    plan: (sm) => ({
        minWidth: `227px`,
        minHeight: `296px`,
        marginTop: `${sm ? '1rem' : ''}`,
    }),
    background: () => ({
        // background: `#CCCCCC 0% 0% no-repeat padding-box`,
        borderRadius: '4px',
        marginBotton: '10px'
    })
}